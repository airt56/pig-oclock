# 小猪举铁钟

双击 Release 中的 **PigLiftClock.exe** 即可运行。本版本为直角界面。无需安装，也不需要 Python 或 Node。程序使用 Windows 的 .NET Framework 4.x。

## 计时

- 专注：默认 25 分钟，小猪举哑铃。
- 短休息：默认 5 分钟，小猪盖被睡觉。
- 长休息：默认 15 分钟，每完成 4 次专注后准备长休息。
- 点击“开始”计时；可暂停、继续、重置。暂停时动画也暂停。
- 点击“设置”可将各阶段设为 1–180 分钟，并开关到点提示音。保存设置会重置当前计时。
- 到点弹出提示，并准备下一阶段；点击“开始”才开始下一轮。
- 最小化后继续计时，可双击右下角托盘小猪图标恢复。勾选“窗口置顶”可一直显示在其他普通窗口上方。
- 关闭窗口会退出程序；再次打开从完整时长开始。程序退出、电脑关机时不会提醒。

## 每日任务

在底部输入任务，点击“添加”或按 Enter。勾选表示完成，点击右侧“×”删除。

右上角日期控件可查看、添加或修改不同日期的任务。跨天时，如果正在显示今天，会自动切换到新的一天；历史任务保留。

任务、时长、静音和置顶设置自动保存在：

`%LOCALAPPDATA%\PigLiftClock\data.xml`

可在文件资源管理器地址栏粘贴 `%LOCALAPPDATA%\PigLiftClock` 打开该文件夹。`data.xml.bak` 是上一次保存的备份。搬到另一台电脑时，复制 EXE；如需迁移任务，请在关闭程序后同时复制数据文件。

## 文件

- `PigLiftClock.exe`：可直接运行的程序。
- `小猪举铁钟-UI效果图.png`：最初确认的设计参考图；正式程序的小猪是可缩放的矢量动画。
- `src`：完整 C# 源码。
- `build.ps1`：重新编译脚本。
- `tests`：测试程序、隔离测试数据及实际窗口截图，不影响日常任务数据。

重新编译：在源码文件夹打开 PowerShell，执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\build.ps1`。输出位于 `dist\PigLiftClock.exe`。
