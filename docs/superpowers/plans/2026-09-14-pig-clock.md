# Pig Clock Implementation Plan

> Execute inline using superpowers:executing-plans.

**Goal:** Deliver a working Chinese Windows timer EXE matching the approved concept.
**Architecture:** Isolated timer and XML storage model; custom drawn animated pig; native form controls.
**Tech Stack:** C# / .NET Framework / WinForms / System.Drawing.
**Spec:** docs/superpowers/specs/2026-09-14-pig-clock-design.md

## Tasks
- [x] Core: tests/CoreTests.cs exercises Countdown.Start/Pause/Tick with explicit UTC timestamps, plus Repository.Save/Load and per-date Tasks. Missing implementation failure observed; src/Core.cs implemented; tests passed.
- [x] UI: src/Artwork.cs paints scalable pig states. src/MainForm.cs implements timer, date picker, checklist, settings, notifications and tray restoration. src/Program.cs provides startup; separate tests/UiTests.cs provides diagnostic rendering. build.ps1 compiles with Framework64/v4.0.30319/csc.exe.
- [x] Verification: core suite and UI runner passed. Focus/rest/compact screenshots inspected. Start/pause/reset/add/check/delete/settings, date isolation, real completion dialog, next stage and pin behavior passed. Build final EXE in current folder and document usage.

## Review and diagnostics
Independent code review found system dialog/notification sounds bypassing the mute switch and a completion status overwritten by ticks. Removed balloon notification, used a no-icon completion dialog, retained completion status until the next user action. UI regression verified completion status.

Sandbox prevented File.Replace in isolated test data; the same test passed with approved execution. Reminder test initially counted repeated polling of one dialog as multiple alerts; tracing showed one window, so the test now counts distinct window handles. Final result: one alert and paused next stage.

## Constraints
No network dependencies. Preserve reference image. Never alter user tasks in diagnostics; use isolated test data. Read/save errors are surfaced, not silently discarded. UI timer uses wall-clock deadline instead of subtracting tick counts.
